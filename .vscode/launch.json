{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Scraper",
            "type": "python",
            "request": "launch",
            "module": "scraper.main_scraper",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
